{% for host in groups['servers'] %}
{% if ansible_hostname != hostvars[host].ansible_hostname %}

{{ansible_default_ipv4.address}}  {{hostvars[host].ansible_default_ipv4.address}}   : PSK "{{psk.stdout}}"


{% endif %}

{% endfor %}